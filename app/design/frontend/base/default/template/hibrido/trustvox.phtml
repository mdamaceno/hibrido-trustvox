<?php
$posicao = Mage::getStoreConfig('hibridotrustvox/configuracoes/posicao_do_widget');

if ($posicao == 'padrao') {
    echo $this->getLayout()
        ->createBlock('core/template')
        ->setTemplate('hibrido/widget_trustvox.phtml')
        ->toHtml();
}
?>
